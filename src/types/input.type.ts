export type CheckboxSelectType = 'on' | 'off' | 'indeterminate';

export type CheckboxMode = 'on-off' | 'indeterminate-off' | 'full-cycle';

export type RadioButtonOption = {
    label: string;
    value: string;
};
